/* WORKLINK SA — Firebase configuration
   Paste your Firebase Web App configuration here when Phase 1 begins.
   Keep this file deployed; Firebase client config is not a secret. Security comes from Firebase Rules + server-side functions.
*/
window.WORKLINK_FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};